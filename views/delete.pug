extends layout

block content
  h1 Delete User

    form( name="but_delete")
    
        input(
        type="text"
        name="email"
        id="but_delete_email"
        placeholder="E-mail"
        )

        input(
        type="text"
        name="username"
        id="but_delete_username"
        placeholder="username"
        )

        input(
        type="password"
        name="password"
        id="but_delete_password"
        placeholder="password"
        )

        input(
        type="submit"
        id="but_delete" 
        )

  div
    button(onclick="window.location.href='/'") Main Page
    button(onclick="window.location.href='/accountCreation'") Create Account